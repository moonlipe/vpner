# Código do daemon — clonado via CI, não versionado aqui
vpn-daemon/

# Credenciais e configs reais
*.env
!forti-daemon.env.example
*.toml
!snx-config.example.toml

# Sessão do navegador e cookies
**/saml_cookie
**/browser_state.json
**/.local/

# Builds e temporários
*.tar.gz
.claude/